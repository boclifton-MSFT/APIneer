# Squad: ignore runtime state (logs, inbox, sessions)
.squad/orchestration-log/
.squad/log/
.squad/decisions/inbox/
.squad/sessions/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream

# .NET
bin/
obj/
*.user
*.suo

# Node / Frontend
node_modules/
.nuxt/
.output/
dist/

# Data (runtime SQLite db location only — anchored to repo root)
/data/
# Local SQLite database files (in case they land elsewhere)
*.db
*.db-shm
*.db-wal

# Environment
.env
.env.local

# IDE
.vs/
*.swp

**/*.lscache